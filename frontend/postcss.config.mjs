/**
 * @file postcss.config.mjs
 * @description PostCSS configuration for Tailwind CSS v4.
 *              Uses the official @tailwindcss/postcss plugin (v4 approach).
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
